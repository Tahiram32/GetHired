{
  "name": "GetHired Env",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "forwardPorts": [5173, 8001],
  "postCreateCommand": "npm install && cd backend && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": ["esbenp.prettier-vscode", "ms-python.python"]
    }
  }
}
